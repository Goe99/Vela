# 11 — Board 内搜索与跳去 DSH 的会话搜索

**What to build:** Board 上能按编号、标题、描述找卡，结果即时过滤。另外给一个入口跳去 DSH 自己的会话搜索——找历史会话是 DSH 已经有的能力，按 ADR-0020 不重画。

**Blocked by:** 02 — Issue 形状扩容（要能按编号搜）

**Status:** done

- [x] Board 顶部有搜索框，输入即过滤，六列同时收窄
- [x] 能按编号精确匹配，也能按标题与描述模糊匹配
- [x] 搜索与已有的 Workspace 筛选可叠加
- [x] 清空搜索恢复完整视图
- [x] 无结果时给出明确空状态，而不是六条空泳道
- [x] 搜索状态不落盘——刷新后回到未搜索状态
- [x] 有一个入口跳去 DSH 的会话搜索；若跳不过去，按 ADR-0020 退化为明确的替代动作而不是死按钮

## 完成记录

搜索逻辑是纯函数（`src/domain/search.ts`），因此「什么算命中」被逐条钉住了：编号
精确匹配（`V-12` / `v-12` / `12` 都算），标题与描述子串匹配且大小写不敏感。

**编号刻意不做子串匹配**：输入 `1` 时把 1、10、11、100 全捞出来毫无用处。但纯数字
仍然会去撞标题与描述——那条路径上 `1` 匹配「第 1 步」是对的。

**空查询返回传进来的那个数组本身**，不是副本。这条路径每次轮询都会走，返回新数组
会让下游 memo 全部失效，白白重渲整个看板。

**结果不排序。** 卡片仍待在它原本那一列的原本位置上，因此「相关度排序」没有落脚
处——位置是 Operator 自己排的，搜索不该动它。

### 「跳去 DSH 会话搜索」退化成了什么

ADR-0020 的取证已经确认 DSH **没有**给第三方插件的编程式跳页。所以这个入口做的是
把看板让开（`panel.close()`）——dsh 的会话列表就在它底下。

这不是一个死按钮：它真的把你送到能搜会话的地方，只是没能帮你把搜索词带过去。
按钮上带 title 说明了这一点，而不是让人以为是个 bug。
