# 13 — 加队员弹窗：视觉打磨与双主题验证

**What to build:** 对照 waker-replica/waker-ui-tokens.md 第 4 节的形态参考，打磨弹窗观感：弹窗宽度适配两列卡片网格（现有 modal 宽 560px，卡片拥挤时可微调至约 640px）、弹窗内卡片网格的间距节奏、卡片 hover 反馈清晰。所有颜色、圆角、间距沿用 Vela 的 `--vela-*` 变量，保证明暗两套主题都成立——不引入 Waker 扒取文档里的任何色值。最后在浏览器里把明暗两套主题各过一遍完整验收。

**Blocked by:** 12 — 加队员弹窗：组件与交互集成

**Status:** done

- [x] 弹窗宽度让两列卡片舒展不拥挤；内容超出时弹窗体内可滚动、底栏固定
- [x] 卡片 hover 有明确视觉反馈；明暗主题下卡片边框、文字、背景对比度均正常
- [x] 浏览器实测两种主题：打开弹窗、点卡加人、X / 遮罩 / Esc / 取消关闭，行为全部符合预期
- [x] 现有测试全部通过

## Comments

- 2026-08-19 自 `waker-replica/issues/02-dialog-polish-and-theme-verification.md` 抄入（经 Operator 确认）。浏览器实测由 agent 用自动化完成并截图汇报；自动化点不动的条目转人工。
- 2026-08-19 完成。实测结论：560px 下两列卡片「刚好偏宽松」（明、暗各一轮，均不拥挤），未启用 640px 微调。网格两列固定、间距 10px；滚动与固定底栏由既有 modal 基建（max-height 85vh + body 滚动）结构保证。明暗两套主题各过一遍完整验收（打开 / 点卡加人 / X / 遮罩 / Esc / 取消），全部通过；截图存于 `.dsh-scratch/accept-*.png` 等（gitignored 的验证环境，重启即弃）。未引入任何 Waker 色值。
