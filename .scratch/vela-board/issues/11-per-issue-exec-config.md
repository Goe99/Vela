# 11 — per-Issue 执行配置覆盖

**What to build:** 「改 README」和「改构建脚本」不该共享同一个权限档位。这一票让单张卡片能覆盖 agent preset、sandbox 档位与超时，未覆盖的项回落到全局默认——既保住一键派活的手感，又让高风险任务能被单独收紧。

**Blocked by:** 07 — 派活：Run 启动、Running Lane 与跳转会话

**Status:** done

- [x] 单个 Issue 可覆盖 agent preset、sandbox 档位与超时
- [x] 未覆盖的项回落到全局默认；解析顺序是 Issue 覆盖 → 全局默认
- [x] 全局默认值是插件的配置项，**不是源码常量**
- [x] 派活时**不弹配置对话框**——一键派活的手感不能被牺牲
- [x] 超时由 Vela 自己计时并主动取消 Run
- [x] 取消后仍等待确认真正结束，不假设「取消调用返回」等于「执行已结束」
- [x] 超时导致的结束在卡片上与其他失败原因可区分
- [x] 执行配置解析在 host 侧被单独测试，覆盖各种覆盖与回落组合
